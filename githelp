# just a list of commands to help me remember the command line interface through examples... 

git add README.md
git commit -m "first commit"
git remote add origin https://github.com/edwardspbe/ProjectOllie.git
git config git config --global user.email "paul.e@rogers.com"

git config --global user.email
git config --global user.email "paul.e@rogers.com"

git commit -m "first commit"
git push -u origin master
git add .

#configure user name
git config user.name
git remote -v

#NOTE: ssh keys are required.  these are stored in a locked
#configure ssh location
git remote set-url origin git@github.com:edwardspbe/ProjectOllie.git

#adding files or . (for all new files)
git add banner.jpg 
git add .

#commit additions and changes
git commit -m "adding banner.jpg"

#push commitments to the git server
git push

#single file checkin
git commit -m 'captured first test-case: open/close door' -- ProjectOllie/olliemon

#other cmds
git diff
git show
git status
